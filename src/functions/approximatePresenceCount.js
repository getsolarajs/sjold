module.exports = { name: "$approximatePresenceCount", description: "Alias for $guildApproximatePresenceCount.", takesBrackets: true, execute: async (context, args) => { const guildId = args[0]?.trim(); let targetGuild = null; if (guildId) { try { targetGuild = await context.client.guilds.fetch(guildId); } catch { return `[Error: Could not find guild ${guildId}]`; } } else { targetGuild = context.guild; } if (targetGuild) return targetGuild.approximatePresenceCount?.toString() ?? ""; return "[Error: Could not determine guild]"; } };
