module.exports = { name: "$maxPresences", description: "Alias for $guildMaxPresences.", takesBrackets: true, execute: async (context, args) => {  const guildId = args[0]?.trim(); let targetGuild = null; if (guildId) { try { targetGuild = await context.client.guilds.fetch(guildId); } catch { return `[Error: Could not find guild ${guildId}]`; } } else { targetGuild = context.guild; } if (targetGuild) return targetGuild.maximumPresences?.toString() ?? "25000"; return "[Error: Could not determine guild]"; } };