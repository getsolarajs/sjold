module.exports = { name: "$randomFloat", description: "Returns a random floating-point number between min (inclusive) and max (exclusive). Args: [min=0];[max=1]", takesBrackets: true, execute: async (context, args) => { let min = 0, max = 1; if (args.length === 1) max = parseFloat(args[0]); else if (args.length >= 2) { min = parseFloat(args[0]); max = parseFloat(args[1]); } if (isNaN(min) || isNaN(max)) return "[Error: Invalid number range]"; if (min >= max) return "[Error: min must be less than max]"; return (Math.random() * (max - min) + min).toString(); } };