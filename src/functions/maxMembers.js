module.exports = { name: "$maxMembers", description: "Alias for $guildMaxMembers.", takesBrackets: true, execute: async (context, args) => { const guildId = args[0]?.trim(); let targetGuild = null; if (guildId) { try { targetGuild = await context.client.guilds.fetch(guildId); } catch { return `[Error: Could not find guild ${guildId}]`; } } else { targetGuild = context.guild; } if (targetGuild) return targetGuild.maximumMembers?.toString() ?? ""; return "[Error: Could not determine guild]"; } };